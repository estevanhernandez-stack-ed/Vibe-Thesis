# Vibe Thesis — Build Checklist

> **Inputs:** [`scope.md`](scope.md), [`prd.md`](prd.md), [`spec.md`](spec.md), [`cart-cycle-brief.md`](cart-cycle-brief.md). Generated 2026-04-26 19:44 CST in `'fully-autonomous'` autonomy mode; cart-cycle-brief `/checklist` stance pre-statement is canonical.
>
> **Build-ordering constraint from /spec handoff:** cycle item 1 MUST be hello-world preflight; cycle item 2 MUST be source-repo audit. All extraction work depends on findings from items 1 and 2. Final item is always documentation & security verification.

## Build Preferences

- **Build mode:** **Autonomous** — orchestrator dispatches each item to a subagent. Aligns with cart-cycle-brief stance and the established Estevan pattern across 9 prior Cart cycles.
- **Comprehension checks:** N/A (autonomous mode).
- **Git:** **Commit after each item** with message format `Complete step N: [title]` (Conventional Commits subject style). Auto-commit per item is on (fully-autonomous default). The orchestrator is responsible for staging + committing after each item completes; if the harness owns the commit step, the orchestrator emits a `commit_request` envelope item per checklist completion.
- **Verification:** **Off — summary at the end + structural verification beat before /reflect.** No mid-build verification beats per cart-cycle-brief stance — mid-build verification is friction-laden when cycle scope is bounded extraction work. The structural verification beat at item 11 is the cycle's load-bearing acceptance test.
- **Check-in cadence:** N/A (autonomous mode).
- **Subagent dispatch strategy:**
  - **Sequential (must-finish-before-next):** items 1 → 2 → 3 (manifest depends on item 1 findings); item 4 → 5 (overlay must be byte-identical subset of full); item 8 (orchestrator composes 4-7); item 11 (depends on EVERYTHING); item 12 (final).
  - **Parallel-eligible (after item 2 completes):** items 4, 6, 7 — independent extractions that can fan out.
  - **Parallel-eligible (after item 4 completes):** items 9, 10 — depend on templates payload existing but not on each other.
- **Sibling-repo hard guard:** every `Write`, `Edit`, or `cp -R` invocation in this build MUST refuse paths that resolve to `agentic-architect-vibe`. Build work stays in `c:\Users\estev\Projects\vibe-thesis\`. Extraction = copying out via `gh repo clone` to a temp dir, never modifying source repo in place. Per cart-cycle-brief constraints.

## Checklist

- [ ] **1. Hello-world plugin preflight**
  Spec ref: `spec.md > Hello-World Pre-Flight`
  What to build: Create `/tmp/vibe-thesis-helloworld/` with the exact tree from the spec — `.claude-plugin/plugin.json` with `name: vt-helloworld`, `skills/hello/SKILL.md` with `name: hello, user-invocable: true` frontmatter, `commands/hello.md` stub. Install via `/plugin marketplace add /tmp/vibe-thesis-helloworld` followed by `/plugin install vt-helloworld@vt-helloworld` (or whatever live install verbs map to — record the actual incantation). Verify Claude Code recognizes the plugin (`/plugin list` shows it; `/vt-helloworld:hello` invokes the skill; `/hello` slash command stub also invokes the skill). Log every observed-vs-documented divergence in `process-notes.md` under `## /build cycle 1 — hello-world preflight`. If any fact in `spec.md > Plugin Manifest` is contradicted by observed behavior, update the spec inline before item 2 starts.
  Acceptance: PRD Q1 (BLOCKER) is empirically closed. Hello-world plugin installs cleanly and responds to slash command invocation. Any divergence between spec assumptions and live behavior is documented and applied to spec.md.
  Verify: Run `/plugin list` in Claude Code and observe `vt-helloworld` listed. Invoke `/vt-helloworld:hello` and observe skill response. Confirm `process-notes.md` contains the preflight findings section.

- [ ] **2. Source repo audit — template/main deltas**
  Spec ref: `spec.md > Open Issues > Issue 1 + Issue 2`
  What to build: Use `gh repo clone estevanhernandez-stack-ed/agentic-architect-vibe /tmp/avibe-source/ -- --depth=20` (shallow but enough for `template/main` reach) and `gh repo clone estevanhernandez-stack-ed/ThesisStudio /tmp/thesisstudio-source/`. Run `git diff template/main..HEAD -- ':!03_BODY' ':!02_RESEARCH' ':!01_PLANNING' ':!05_CITATIONS' ':!08_OUTPUT'` inside the avibe clone to isolate infrastructure-only deltas. Confirm the local-contributions list from the originating handoff sketch (§2: PRs #1-#5 + commits `e941da3`, `202345d`, `978426a`) is the complete set. Compare ThesisStudio HEAD against the originating handoff's inventory (§1) and record any drift in `process-notes.md` under `## /build cycle 2 — source audit`. The drift goes into `templates/full/` at item 4. **Sibling-repo hard guard active:** clone to `/tmp/`, never edit either source clone in place.
  Acceptance: Spec Issue 1 (source-repo accessibility) and Issue 2 (ThesisStudio drift) closed. Inventory of files-to-lift is concrete and current as of build time, not handoff time. Drift between handoff inventory and current ThesisStudio is documented.
  Verify: `process-notes.md` contains the source-audit section with two sub-lists: (a) confirmed-from-handoff infrastructure deltas, (b) drift discovered between handoff and build-time. Both `/tmp/avibe-source/` and `/tmp/thesisstudio-source/` exist and contain expected contents.

> **Revision 2026-04-26 20:22 (mid-build, post-source-audit):** Item 6 (plugin-side bootstrap + merge-authors) collapsed — ThesisStudio absorbed all the sub-skills; they ship inside `templates/full/.claude/skills/` automatically. Items 7 + 8 ADDED for voice-synthesis and synthesis-guard per Estevan course-correction. Net 13 items. See process-notes for friction log.

- [ ] **3. Plugin scaffolding — manifest + marketplace.json + repo skeleton**
  Spec ref: `spec.md > Plugin Manifest` + `spec.md > Plugin Layout`
  What to build: In `c:\Users\estev\Projects\vibe-thesis\` create the plugin's repo skeleton: `.claude-plugin/plugin.json` (top-level marketplace pointer if needed) + `.claude-plugin/marketplace.json` + `plugins/vibe-thesis/.claude-plugin/plugin.json` + empty `plugins/vibe-thesis/{commands,skills,templates,examples,docs,architecture}/` directories with `.gitkeep` placeholders + `README.md` + `LICENSE` + `CHANGELOG.md` + `.gitignore`. Both manifest JSONs match the exact shape from `spec.md > Plugin Manifest`, including the `@626labs/vibe-thesis` namespace if pattern-match against vibe-cartographer at clone time confirms (otherwise `estevanhernandez-stack-ed/vibe-thesis`). Initialize git, set the resolved remote, push initial commit. Apply any spec edits from item 1's preflight findings before drafting the manifest.
  Acceptance: Plugin repo exists at the resolved GitHub remote with valid manifest + marketplace.json. `/plugin marketplace add <git-url>` finds the marketplace. PRD Q6 (namespace) is empirically resolved.
  Verify: Run `/plugin marketplace add <repo-url>` against a fresh test environment and confirm marketplace lists `vibe-thesis` plugin. `cat .claude-plugin/plugin.json | jq .name` returns `"vibe-thesis"`.

- [ ] **4. Templates payload — `templates/full/` (ThesisStudio clone + `.gitattributes`)**
  Spec ref: `spec.md > Templates Payload > templates/full/` (revised post-drift)
  What to build: `cp -R c:/tmp/ThesisStudio/* plugins/vibe-thesis/templates/full/`, then `cp -R c:/tmp/ThesisStudio/.* plugins/vibe-thesis/templates/full/` for hidden files (.devcontainer, .github, .husky, .markdownlint.json, .gitignore, .claude). Remove `.git/` from the copied tree. Add `.gitattributes` with `* text=auto eol=lf` (the one local addition not yet in ThesisStudio). Append the `<!-- VIBE_THESIS_MARKER: v0.1.0 -->` stanza to the copied `CLAUDE.md` (currently `[YOUR THESIS TITLE]` placeholder version) so the orchestrator can detect Vibe-Thesis-scaffolded projects. Sub-skills (`bootstrap`, `merge-authors`, `lay-translator`, `research-integrate`) ship as project-local skills automatically inside `templates/full/.claude/skills/`. **Do NOT generalize beyond ThesisStudio name** — ThesisStudio's bootstrap already uses clean placeholders ([YOUR THESIS TITLE], [YOUR_NAME]) and properly attributes itself ("Forked from ThesisStudio").
  Acceptance: `templates/full/` is a current ThesisStudio clone + `.gitattributes` + VIBE_THESIS_MARKER stanza. All 4 ThesisStudio sub-skills present. ~103 files (102 from ThesisStudio + 1 .gitattributes).
  Verify: `find plugins/vibe-thesis/templates/full/ -type f | wc -l` returns ~103. `cat plugins/vibe-thesis/templates/full/.gitattributes` shows `eol=lf` rule. `grep VIBE_THESIS_MARKER plugins/vibe-thesis/templates/full/CLAUDE.md` returns 1 line. `ls plugins/vibe-thesis/templates/full/.claude/skills/` shows bootstrap, merge-authors, lay-translator, research-integrate.

- [ ] **5. Templates payload — `templates/overlay/` (just `.gitattributes` + VIBE_THESIS_MARKER) + invariant check**
  Spec ref: `spec.md > Templates Payload > templates/overlay/` (revised post-drift)
  What to build: `templates/overlay/` contains the local-additions diff between current ThesisStudio HEAD and what Path A produces: `.gitattributes` (the `eol=lf` rule) and a script that appends the VIBE_THESIS_MARKER stanza to the spawned project's CLAUDE.md. Both are byte-identical to their `templates/full/` counterparts — `cp` from `templates/full/` rather than re-author. Author `scripts/check-overlay-invariant.sh` that diffs every file in overlay/ against its full/ counterpart and exits 1 on mismatch.
  Acceptance: Overlay is one file (`.gitattributes`) plus the marker-injection script. Invariant satisfied trivially. Path B = `gh repo create --template ThesisStudio` + `cp templates/overlay/.gitattributes .` + run the marker-injection script = byte-equivalent to Path A modulo .git.
  Verify: `ls plugins/vibe-thesis/templates/overlay/` shows just the expected files. `bash scripts/check-overlay-invariant.sh` exits 0. `diff plugins/vibe-thesis/templates/full/.gitattributes plugins/vibe-thesis/templates/overlay/.gitattributes` produces no output.

- [ ] **~~6. Sub-skills — bootstrap + merge-authors~~ COLLAPSED**
  Per 2026-04-26 20:22 revision: ThesisStudio absorbed all sub-skills (`bootstrap`, `merge-authors`, `lay-translator`, `research-integrate`). They ship as project-local skills inside `templates/full/.claude/skills/` (item 4) and are dispatched by the orchestrator via the Skill tool to project-local namespace. The plugin does NOT fork them.

- [ ] **7. Slash commands — vibe-render + vibe-status (stubs + skills)**
  Spec ref: `spec.md > Component: Slash Command /vibe-thesis:vibe-render` + `spec.md > Component: Slash Command /vibe-thesis:vibe-status`
  What to build: Create four files: `plugins/vibe-thesis/commands/vibe-render.md` (lightweight stub with `argument-hint: "[pdf|html|md|all]"`), `plugins/vibe-thesis/skills/vibe-render/SKILL.md` (actual content per spec — verifies marker, parses arg, runs compile-tokens + check-citations pre-steps, runs `npm run render:<format>`, surfaces stderr with hints on failure), `plugins/vibe-thesis/commands/vibe-status.md` (lightweight stub), `plugins/vibe-thesis/skills/vibe-status/SKILL.md` (actual content — composes one-screen project-state report per spec). Both skills' frontmatter has `user-invocable: true` and appropriate `allowed-tools` (`Bash(npm run *) Read` for vibe-render).
  Acceptance: Slash commands install per the vibe-cartographer pattern. Both skills have the correct frontmatter + behavior contract. Marker-not-present case is handled (refuse with clear instruction to scaffold first).
  Verify: After installing the in-progress plugin into a test directory with a marker present, `/vibe-thesis:vibe-render pdf` invokes the render pipeline. `/vibe-thesis:vibe-status` produces a one-screen summary. Both refuse cleanly when invoked in a non-marker directory.

- [ ] **7b. NEW: Voice-synthesis skill + slash command `/vibe-thesis:voice`**
  Spec ref: `spec.md > Component: Voice Synthesis` (NEW section, authored alongside this item)
  What to build: Two files — `plugins/vibe-thesis/commands/voice.md` (lightweight stub with `argument-hint: "[revise]"`) and `plugins/vibe-thesis/skills/voice-synthesis/SKILL.md`. Skill behavior: verifies VIBE_THESIS_MARKER present; reads the project's scope (if scaffolded by Vibe Thesis after a Cart cycle, falls back to the topic from CLAUDE.md if not); interviews the user one question at a time on three axes — (1) "Name 2-4 authors whose prose you admire (any era, any field)," (2) "Name 2-4 contemporary field experts whose voice shapes current discourse in your topic," (3) "What's the synthesis ratio: anchor more in timeless voices, more in current experts, or even split?". Writes a `## VOICE SYNTHESIS` block to CLAUDE.md naming the authors + experts + ratio + explicit synthesis-not-imitation instruction. Idempotent — re-runnable to revise.
  Acceptance: New plugin slash command `/vibe-thesis:voice` works; appends correctly-formatted VOICE SYNTHESIS block to CLAUDE.md; LeadWriter persona reading CLAUDE.md will pick up the synthesis instruction at drafting time.
  Verify: After installing the in-progress plugin into a Vibe-Thesis-scaffolded test directory, `/vibe-thesis:voice` runs the interview to completion and `grep -c "## VOICE SYNTHESIS" CLAUDE.md` returns 1.

- [ ] **7c. NEW: Synthesis-guard skill + slash command `/vibe-thesis:guard`**
  Spec ref: `spec.md > Component: Synthesis Guard` (NEW section, authored alongside this item)
  What to build: Two files — `plugins/vibe-thesis/commands/guard.md` (lightweight stub with `argument-hint: "[strict|standard]"`) and `plugins/vibe-thesis/skills/synthesis-guard/SKILL.md`. Skill behavior: verifies VIBE_THESIS_MARKER present; reads each `03_BODY/*.md` (or `03_BODY/<chapter>/*.md` for dissertation/masters mode); scans for self-review patterns — inflationary language ("groundbreaking", "comprehensive", "rigorous", "definitive", "novel", "state-of-the-art"), self-praise framings ("we make N contributions", "to the best of our knowledge", "no prior work", "our approach uniquely"), defensive over-qualification ("despite limitations, our findings demonstrate"), conclusions that re-assert importance. Reports findings as a structured list: `<file>:<line> <pattern>: <quoted text> → <one-line suggestion>`. Doesn't auto-fix. `strict` mode = full pattern set; `standard` (default) = inflationary + self-praise only. Pre-render integration: `/vibe-thesis:vibe-render` checks for a `--guard` flag and runs synthesis-guard as a pre-step if set; surfaces findings, asks builder to acknowledge or fix before proceeding.
  Acceptance: New plugin slash command `/vibe-thesis:guard` runs the lint pass, produces structured findings, exits 0 (always — it's advisory, not gate). Pre-render integration via `--guard` flag works. ThesisStudio's third pillar default (Honest Limits) becomes enforceable, not aspirational.
  Verify: After installing the in-progress plugin into a Vibe-Thesis-scaffolded test directory, write a `03_BODY/test.md` containing "We make three groundbreaking contributions, to the best of our knowledge no prior work has comprehensively addressed this rigorous problem." Then `/vibe-thesis:guard strict` reports at least 4 findings (groundbreaking, contributions, to the best of our knowledge, comprehensively, rigorous — all inflationary or self-praise). Re-running on a clean file (no offenders) reports zero findings and exits cleanly.

- [ ] **8. Orchestrator skill `vibe-thesis`** (revised post-drift + voice/guard additions)
  Spec ref: `spec.md > Component: Orchestrator Skill (skills/vibe-thesis/)` (decision tree + phase detection, revised)
  What to build: Create `plugins/vibe-thesis/skills/vibe-thesis/SKILL.md` implementing the revised decision tree — invocation detection (cwd state + marker grep), scaffold-mode (path detection via `gh --version && gh auth status`, Path A `cp -R templates/full/. ./`, Path B `gh repo create --template estevanhernandez-stack-ed/ThesisStudio <name> && cp -R templates/overlay/. ./` + marker-injection script, fallback to Path A on `gh` failure). **Bootstrap dispatch:** orchestrator dispatches to **project-local** `/bootstrap` (lifted from ThesisStudio in templates/full/.claude/skills/bootstrap/) — Skill tool resolves to project namespace, not plugin namespace. **Post-bootstrap:** orchestrator dispatches to plugin-side `/vibe-thesis:voice` to set the author voice. Toolchain install one-question prompt. Round-trip confirmation copying `examples/demo-article/` into scaffold and running `npm run render:pdf`. Iterate-mode phase detection from artifact state. Sub-skill / slash-command dispatch on natural-language intent — including new "check for self-review tone" → `/vibe-thesis:guard`. Frontmatter: `name: vibe-thesis`, `user-invocable: false`. Marker-fallback structural detection. Sibling-repo guard: refuse any path under `agentic-architect-vibe`.
  Acceptance: Orchestrator implements PRD Epics 1, 3, 7, 9 plus the new voice-synthesis post-bootstrap flow and guard offering at iterate. Auto-detection works. Project-local bootstrap dispatch resolves correctly. Voice-synthesis runs after bootstrap. Round-trip confirmation succeeds end-to-end.
  Verify: After installing the in-progress plugin into a fresh test directory, say "scaffold a vibe thesis project for me" — orchestrator runs scaffold → project-local /bootstrap → /vibe-thesis:voice → toolchain install → round-trip confirmation. Re-invoke in scaffolded directory; switches to iterate-mode.

- [ ] **9. Worked example — `examples/demo-article/`**
  Spec ref: `spec.md > Worked Example`
  What to build: Author the bundled example at `plugins/vibe-thesis/examples/demo-article/` per spec — `01_PLANNING/proposal.md` (~1 page, "A Brief Examination of Rubber Ducks in Software Engineering"), `01_PLANNING/outline.md` (3-section outline), `01_PLANNING/claims-map.md` (5 claims with referents), `03_BODY/01-introduction.md` + `03_BODY/02-methods.md` + `03_BODY/03-conclusion.md` (~1 page each), `05_CITATIONS/references.bib` with 5 real entries (Brooks, Naur, Hunt-Thomas, Norman, Knuth — all real and citable), pre-rendered `08_OUTPUT/pdf/example.pdf` committed for diff comparison, `manifest.json` for v0.2-readiness sync detection. Content must be intentionally low-stakes so the demo doesn't accidentally read as Estevan's authoritative position. Run `npm run render:pdf` from a temp scaffold that copied this example in, confirm PDF lands cleanly, copy result to `08_OUTPUT/pdf/example.pdf` for the bundled artifact.
  Acceptance: Demo article round-trips through render pipeline. PDF is publishable-quality (typography looks editorial-grade, not generic Pandoc output). Content is light enough not to misrepresent Estevan.
  Verify: From a fresh scaffolded test project that copied the demo into the numbered scaffold, `npm run render:pdf` exits 0 and produces a PDF that's byte-similar (modulo PDF metadata timestamps) to the bundled `examples/demo-article/08_OUTPUT/pdf/example.pdf`.

- [ ] **10. Plugin docs — condensed `architecture.md` + plugin-level CLAUDE.md**
  Spec ref: `spec.md > ADR Lift Strategy` + `spec.md > Plugin Layout > docs/architecture.md`
  What to build: Author `plugins/vibe-thesis/docs/architecture.md` as a single condensed ADR per spec — 7 sections (Overview, Templates payload, Render pipeline, Design system single-source-of-truth, Mode flag plumbing, Sub-skill composition, Dual scaffold path tradeoffs). Lift core content from the 5 source-repo ADRs but condense aggressively — plugin docs are read by users browsing the marketplace listing, not by future maintainers iterating on internals. Also author `plugins/vibe-thesis/CLAUDE.md` (the plugin-level instructions Claude loads when the plugin is invoked) — short, references the orchestrator skill as the entry point, and points to `architecture/default-patterns.md` for fallback patterns. Author `plugins/vibe-thesis/architecture/default-patterns.md` as a stub that explains the plugin doesn't impose a stack on the user's project — the templates payload IS the stack.
  Acceptance: `architecture.md` reads as gestalt-over-history; `architecture/default-patterns.md` is honest about the plugin not imposing patterns; plugin-level `CLAUDE.md` orients Claude correctly when the plugin is loaded.
  Verify: `wc -l plugins/vibe-thesis/docs/architecture.md` is between 100-300 lines (concise but real). `cat plugins/vibe-thesis/CLAUDE.md` shows clear orientation to orchestrator skill.

- [ ] **11. Structural verification beat — install + scaffold both paths + tree-equivalence diff**
  Spec ref: `spec.md > Structural Verification Beat`
  What to build: Push the in-progress plugin to its GitHub remote (or use a local marketplace path for speed). Open Claude Code in `/tmp/vibe-thesis-verify-pathA/`, run `gh logout` first to force Path A, install the plugin via `/plugin marketplace add <url>` + `/plugin install vibe-thesis`, say "scaffold a vibe thesis project on rubber duck debugging," confirm scaffold-mode runs end-to-end and `npm run render:pdf` produces `08_OUTPUT/pdf/example.pdf` with no manual fixups. Repeat in `/tmp/vibe-thesis-verify-pathB/` after `gh login` to force Path B; same acceptance. Run `diff -r --exclude='.git' --exclude='node_modules' --exclude='08_OUTPUT' /tmp/vibe-thesis-verify-pathA /tmp/vibe-thesis-verify-pathB` — expected zero diffs. Document any failures in `process-notes.md` under `## /build cycle 11 — structural verification`. **On failure of any beat:** open an /iterate beat to debug rather than handing to next-builder-action (per cart-cycle-brief stance).
  Acceptance: Single non-negotiable acceptance criterion (round-trip on first install, no fixups) is empirically met for BOTH scaffold paths. Tree-equivalence diff between paths is clean.
  Verify: Both `/tmp/vibe-thesis-verify-path{A,B}/08_OUTPUT/pdf/example.pdf` exist and open as valid PDFs. `diff -r ...` exits 0.

- [ ] **12. Documentation & security verification**
  Spec ref: `prd.md > What We're Building` + `spec.md > Deployment — Identity & Signing` + all spec sections
  What to build: Write `README.md` at the plugin repo root covering what Vibe Thesis does, how to install (marketplace + raw git URL), basic usage ("scaffold a vibe thesis project for me"), the dual scaffold path explanation, the dev container vs native install note, the link to `docs/architecture.md` for deeper understanding, screenshots of a freshly-scaffolded project + rendered PDF (capture during item 11). Confirm all `docs/` artifacts in the project (this `c:\Users\estev\Projects\vibe-thesis\docs/` folder containing scope, prd, spec, checklist, cart-cycle-brief, builder-profile) are up to date — if anything changed during build, fix the docs to reflect reality. Author `CHANGELOG.md` for v0.1.0 release notes. Author `LICENSE` (MIT, attribution to 626Labs LLC). Run `npm audit` against `plugins/vibe-thesis/templates/full/package.json` (NOT against the plugin repo itself, which has zero npm deps) — flag and address critical / high vulnerabilities. Secrets scan: `git log --all -p | grep -iE "password|secret|api[_-]?key|token" | grep -v "EXAMPLE\|placeholder\|test"` — confirm zero matches in committed code. Verify `.gitignore` covers `node_modules/`, `08_OUTPUT/`, `.env`, `*.log`. Author `.env.example` with no env vars (the plugin itself needs none) but document that scaffolded projects may need `BIBER_PATH` if biber isn't on PATH. Create the GitHub repo if it doesn't exist (`gh repo create`); push final code; tag `v0.1.0`. Submit plugin to Anthropic plugin marketplace per their listing process.
  Acceptance: README is clear enough that a fresh user can install + scaffold without prior knowledge. All `docs/` artifacts are current. No secrets in git history. Dependency audit shows no unaddressed critical/high vulns. v0.1.0 tag exists. Plugin is submitted to marketplace (or marketplace listing PR is open).
  Verify: Clone the repo into a fresh directory, follow the README, install the plugin, scaffold a project. The user-experience matches what the README promised. `git log --all -p | grep -iE "password|secret|api[_-]?key|token" | grep -v "EXAMPLE\|placeholder\|test"` returns zero. `gh release list` shows `v0.1.0`.
