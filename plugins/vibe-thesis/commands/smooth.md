---
description: "Apply the project's voice profile to a draft markdown file. Multi-pass rewrite (micro / rhythm / macro / exemplar-comparison) preserving claims and structure. Output to <draft>.smoothed.md. The apply step in the voice pipeline (capture → apply → lint)."
argument-hint: "<draft-path> [--passes=micro,rhythm,macro,all] [--exemplars=...] [--register=...] [--paragraphs=N-M] [--dry-run]"
---

Use the **synthesis-smooth** skill to run the multi-pass voice rewrite.
