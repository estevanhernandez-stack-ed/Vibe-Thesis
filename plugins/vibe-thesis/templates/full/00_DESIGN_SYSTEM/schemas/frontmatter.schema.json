{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/estevanhernandez-stack-ed/ThesisStudio/schemas/frontmatter.schema.json",
  "title": "ThesisStudio Body Frontmatter",
  "description": "Optional frontmatter on body markdown files. Used to override default chapter ordering or label.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "title": { "type": "string" },
    "order": { "type": "integer", "minimum": 0, "description": "Override numeric ordering inferred from filename" },
    "label": { "type": "string", "description": "Custom heading label (e.g., 'Chapter 3: Methodology')" },
    "appendix": { "type": "boolean", "description": "Marks this file as an appendix (rendered after main body)" },
    "draft": { "type": "boolean", "description": "If true, excluded from renders" }
  }
}
